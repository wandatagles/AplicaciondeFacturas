# Variables de entorno para FACTUYA (NUNCA subir .env real)
# Copia este archivo a `.env` y completa con tus claves reales.

# Clave de LLMWhisperer (no compartir):
LLMWHISPERER_API_KEY=TU_API_KEY_WHISPER_AQUI

# Clave de OpenAI (no compartir):
OPENAI_API_KEY=TU_API_KEY_OPENAI_AQUI

# Modelo por defecto (puedes ajustar)
OPENAI_MODEL=gpt-4o-mini

# Nivel de logging: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
